#include <stdio.h>
#include <stdlib.h>
#include <time.h>

// min + rand() % (max - min + 1);
// *t++ char *token = strtok(line, ","); fgets(destination, size, pointer source) fgets(temp, size, fileptr) != NULL && x < ROWS

int main(void){



    return 0;
}
